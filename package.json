{
  "name": "erp-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "api",
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter api dev",
    "build": "pnpm --filter api build",
    "start": "pnpm --filter api start"
  }
}
